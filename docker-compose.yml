version: '3.8'

networks:
  kludatabase:
    external: true

services:
  yawshop:
    image: yawshop:latest
    container_name: yawshop
    networks:
      - kludatabase
    ports:
      - "80:5000"
    env_file:
      - .env
    restart: unless-stopped
    
